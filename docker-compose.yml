version: "3.9"

services:
  access-service:
    build:
      context: ./src
      dockerfile: Dockerfile
    container_name: access-service
    ports:
      - "5000:5000"
    restart: unless-stopped
    volumes:
      - "./src/data:/app/data"